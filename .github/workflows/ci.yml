name: DevOps Pipeline

on:
push:
branches:
-main

jobs:
build:
runs-on: ubuntu-latest

steps:
-name: checkout code
-uses: actions/checkout@v3

-name: run a simple script
-uses: echo "Hello, DevOps CI/CD Pipeline is working!"
